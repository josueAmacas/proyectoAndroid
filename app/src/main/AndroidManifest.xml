<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.macasjosue">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".modelo.config.AplicacionORM"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true"/>

        <activity android:name=".Vista.actividades.ActivityMapa"></activity>
        <activity android:name=".Vista.actividades.ActivitySWUsuariovolly" />
        <activity android:name=".Vista.actividades.ActivitySWHilousuarios" />
        <activity android:name=".Vista.actividades.ActivitySWAlumnovolly" />
        <activity android:name=".Vista.actividades.ActivitySWleerjson" />
        <activity android:name=".Vista.actividades.ActivitySWLumen" />
        <activity android:name=".Vista.actividades.ActivityCarroORM" />
        <activity android:name=".Vista.actividades.ActivityArchivos" />
        <activity android:name=".Vista.actividades.ActivitySueldos" />
        <activity android:name=".Examen.ActivityExamennotas" />
        <activity android:name=".Vista.actividades.ActivityProductohelper" />
        <activity android:name=".Vista.actividades.ActivityFragment2" />
        <activity
            android:name=".ActivityNavigationdrawer"
            android:label="@string/title_activity_navigationdrawer"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Vista.fragmentos.fragmento" />
        <activity android:name=".Vista.actividades.Activity_Recibirparametros" />
        <activity android:name=".Vista.actividades.ActivityEnvparametros" />
        <activity android:name=".Vista.actividades.ActivitySuma" />
        <activity android:name=".Vista.actividades.ActivityLogin" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="AA_DB_NAME"
            android:value="movil5a" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1" />
        <meta-data
            android:name="AA_MODELS"
            android:value="com.example.macasjosue.modelo.Carro" />
    </application>

</manifest>